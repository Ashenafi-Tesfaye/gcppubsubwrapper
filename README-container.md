# Build and run the containerized proxy service

# Build the Docker image
# docker build -t pubsub-proxy .

# Run the container (replace PROJECT_ID as needed)
# docker run -p 8080:8080 pubsub-proxy --project=<YOUR_GCP_PROJECT_ID> --port=8080
